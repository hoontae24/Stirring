// import element from './element-ui';
import components from './components';
// import filters from './filters';

export default ({ app }) => {
  // element({ app });
  components({ app });
  // filters({ app });
};
