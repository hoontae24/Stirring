// import element from './element-ui';
import components from './components';
import mixins from './mixins';

export default ({ app }) => {
  // element({ app });
  components({ app });
  mixins({ app });
};
