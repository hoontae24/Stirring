// import element from './element-ui';
import components from './components';
import properties from './properties';

export default ({ app }) => {
  // element({ app });
  components({ app });
  properties({ app });
};
